class Node {
    constructor(account) {
      this.account = account; // Store the account data
      this.next = null; // Points to the next node
    }
  }
  
  export default Node; // Make sure it's exported as default
  